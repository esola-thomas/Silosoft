/**
 * Unit Test: RNG deterministic sequence
 * We will later import the rng module (backend/src/game/rng.ts). For now we just specify desired behavior.
 */

describe('RNG deterministic sequence', () => {
  it('produces the same first N numbers for identical seeds (placeholder)', () => {
    // Placeholder: once rng implemented, require and compare arrays.
    // Example (future): const rng1 = createRng('seed'); const rng2 = createRng('seed');
    // for (let i=0;i<5;i++) expect(rng1.next()).toBe(rng2.next());
    expect(true).toBe(true); // Will be replaced with real assertions.
  });
});
