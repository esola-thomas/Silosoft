/**
 * Unit Test: Layoff event randomness uniformity (minimal statistical check)
 * Placeholder: After implementing event RNG, run many simulations and ensure distribution of affected player ~ uniform.
 */

describe('Layoff event randomness', () => {
  it('placeholder statistical uniformity test', () => {
    // Future: simulate n events, count hits, expect variance within tolerance.
    expect(true).toBe(true);
  });
});
